import { db } from "@/lib/db"; import { DataTable } from "@/components/DataTable";
export default async function Page(){const data=await db.person.findMany({where:{OR:[{notes:null},{notes:{not:{contains:"[DO_NOT_CONTACT_REMOVED]"}}}]},include:{assignments:{where:{active:true,event:{startsAt:{gte:new Date()},canceled:false}},include:{event:true}}},orderBy:{displayName:"asc"}});return <><h1>People</h1><DataTable columns={["Person","Contact","Role","Upcoming","Status"]} rows={data.map(p=>[<b key="n">{p.displayName}</b>,<span key="c">{p.email??"No email"}<br/>{p.phone??"No phone"}</span>,p.role,<span key="a">{p.assignments.map(a=><span key={a.id}>{a.event.name} · {a.event.startsAt.toLocaleDateString()}<br/></span>)}</span>,p.paused?"Paused":p.active?"Active":"Inactive"])}/></>}
