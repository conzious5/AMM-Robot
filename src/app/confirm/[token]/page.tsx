import { notFound, redirect } from "next/navigation";
import { getConfirmation, confirmWithToken } from "@/lib/confirmation";
export default async function Page({params}:{params:Promise<{token:string}>}){const {token}=await params,record=await getConfirmation(token);if(!record||record.usedAt||record.revokedAt||record.expiresAt<=new Date())notFound();const a=record.assignment;async function confirm(){"use server";const result=await confirmWithToken(token);redirect(result.role==="VIDEOGRAPHER"?"https://authentic-moments.com/video-thank-you-for-confirming/":"https://authentic-moments.com/photo-thank-you-for-confirming/")}return <main className="login card"><h1>Confirm your assignment</h1><p><b>{a.person.firstName}</b>, please review these details:</p><h2>{a.event.name}</h2><p>{a.role.toLowerCase()} · {a.event.startsAt.toLocaleString("en-US",{dateStyle:"full",timeStyle:"short",timeZone:a.event.timezone})}</p><p>{a.event.venueName??"Venue details pending"}<br/>{a.event.address}</p><form action={confirm}><button>Confirm Assignment</button></form><p className="muted">Opening this page does not confirm you. Confirmation happens only when you press the button.</p></main>}
